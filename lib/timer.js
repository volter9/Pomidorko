var events = require('../helpers/events');

/**
 * Main logic for timing 
 * 
 * 
 */
var Timer = function () {
    this.startTime = null;
    this.endTime   = null;
    
    this.timer = null;
};

events(Timer.prototype);

Timer.prototype.start = function () {
    
};

Timer.prototype.pause = function () {
    
};

Timer.prototype.stop = function () {
    
};